@using Microsoft.AspNetCore.Identity
@using ServConnect.Models
@inject SignInManager<Users> SignInManager
@inject UserManager<Users> UserManager
@inject Microsoft.AspNetCore.Mvc.Localization.IViewLocalizer Localizer

@{
    var isLoggedIn = SignInManager.IsSignedIn(User);
    Users? currentUser = null;
    var displayName = "User";
    var avatarUrl = string.Empty;
    var currentCulture = System.Globalization.CultureInfo.CurrentUICulture.Name;
    var returnUrl = Context.Request.Path + Context.Request.QueryString;

    if (isLoggedIn)
    {
        currentUser = await UserManager.GetUserAsync(User);
        displayName = currentUser?.FullName ?? currentUser?.Email ?? "User";
        avatarUrl = !string.IsNullOrWhiteSpace(currentUser?.ProfileImageUrl)
            ? currentUser.ProfileImageUrl
            : ($"https://ui-avatars.com/api/?name={Uri.EscapeDataString(displayName)}&background=10b981&color=fff&size=128");
    }
}

<ul class="navbar-nav">
    @if (isLoggedIn)
    {
        if (ViewBag.RestrictNav == true)
        {
            <!-- Restricted mode: only Logout -->
            <li class="nav-item">
                <form class="form-inline" asp-controller="Account" asp-action="Logout" method="post">
                    <button type="submit" class="nav-link btn btn-link text-danger">
                        <i class="bi bi-box-arrow-right"></i> @Localizer["Logout"]
                    </button>
                </form>
            </li>
        }
        else
        {
            @* OTP Notification Bell for Users *@
            @if (User.IsInRole(RoleTypes.User))
            {
                <li class="nav-item position-relative me-2 dropdown">
                    <a class="nav-link" href="#" id="otpNotificationBell" onclick="toggleOtpNotifications()" style="position: relative;">
                        <i class="fas fa-bell" style="font-size: 1.2rem;"></i>
                        <span id="otpNotificationBadge" class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger" style="display: none; font-size: 0.6rem;">
                            0
                        </span>
                    </a>
                    <!-- OTP Notifications Dropdown -->
                    <div id="otpNotificationsDropdown" class="dropdown-menu dropdown-menu-end p-0" style="display: none; min-width: 320px; max-height: 400px; overflow-y: auto; position: absolute; right: 0; left: auto;">
                        <div class="dropdown-header d-flex justify-content-between align-items-center">
                            <span><i class="fas fa-key me-1"></i>Service Start Codes</span>
                            <small id="otpNotificationCount" class="text-muted">0 active</small>
                        </div>
                        <div id="otpNotificationsList">
                            <div class="dropdown-item-text text-center text-muted py-3">
                                <i class="fas fa-info-circle me-1"></i>No active service codes
                            </div>
                        </div>
                    </div>
                </li>
            }
            
            @* Profile dropdown with enhanced styling *@
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false" style="padding: 4px 8px; border-radius: 50px; background: rgba(255, 255, 255, 0.15); margin-left: 8px; transition: all 0.3s ease;">
                    <img src="@avatarUrl" alt="@displayName" class="rounded-circle border" style="width:32px;height:32px;object-fit:cover;border: 2px solid white;" />
                </a>
                <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown" style="margin-top: 8px; border-radius: 10px; box-shadow: 0 8px 16px rgba(0, 0, 0, 0.15);">
                    <li class="dropdown-header">@displayName</li>
                    <li><a class="dropdown-item" asp-controller="Account" asp-action="Profile"><i class="bi bi-person"></i> @Localizer["Profile"]</a></li>
                    <li><a class="dropdown-item" asp-controller="Account" asp-action="ProfileUpdate"><i class="bi bi-pencil-square"></i> Update Details</a></li>
                    <li><hr class="dropdown-divider" /></li>
                    @* Language toggle *@
                    @if (!string.Equals(currentCulture, "ml-IN", System.StringComparison.OrdinalIgnoreCase))
                    {
                        <li>
                            <a class="dropdown-item" asp-controller="Language" asp-action="Set" asp-route-culture="ml-IN" asp-route-returnUrl="@returnUrl">
                                <i class="bi bi-translate"></i> മലയാളത്തിലേക്ക് മാറ്റുക
                            </a>
                        </li>
                    }
                    else
                    {
                        <li>
                            <a class="dropdown-item" asp-controller="Language" asp-action="Set" asp-route-culture="en" asp-route-returnUrl="@returnUrl">
                                <i class="bi bi-translate"></i> Switch to English
                            </a>
                        </li>
                    }
                    <li><hr class="dropdown-divider" /></li>
                    <li>
                        <form class="form-inline px-3" asp-controller="Account" asp-action="Logout" method="post">
                            <button type="submit" class="dropdown-item text-danger"><i class="bi bi-box-arrow-right"></i> @Localizer["Logout"]</button>
                        </form>
                    </li>
                </ul>
            </li>
        }
    }
    else
    {
        @* Language toggle for anonymous users *@
        @if (!string.Equals(currentCulture, "ml-IN", System.StringComparison.OrdinalIgnoreCase))
        {
            <li class="nav-item">
                <a class="nav-link" asp-controller="Language" asp-action="Set" asp-route-culture="ml-IN" asp-route-returnUrl="@returnUrl">
                    <i class="bi bi-translate"></i> മലയാളത്തിലേക്ക് മാറ്റുക
                </a>
            </li>
        }
        else
        {
            <li class="nav-item">
                <a class="nav-link" asp-controller="Language" asp-action="Set" asp-route-culture="en" asp-route-returnUrl="@returnUrl">
                    <i class="bi bi-translate"></i> Switch to English
                </a>
            </li>
        }
        <li class="nav-item">
            <a class="nav-link" asp-controller="Account" asp-action="Register">
                <i class="bi bi-person-plus"></i> @Localizer["Register"]
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link" asp-controller="Account" asp-action="Login">
                <i class="bi bi-box-arrow-in-right"></i> @Localizer["Login"]
            </a>
        </li>
    }
</ul>
