@model ServConnect.ViewModels.ElderProfileSetupViewModel
@{
    ViewData["Title"] = "Edit Profile";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var isMalayalam = System.Globalization.CultureInfo.CurrentUICulture.Name.Equals("ml-IN", StringComparison.OrdinalIgnoreCase);
}

<style>
    .edit-container {
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
    }
    
    .edit-header {
        text-align: center;
        background: linear-gradient(135deg, #bdc3c7 0%, #2c3e50 100%);
        color: white;
        padding: 30px;
        border-radius: 20px;
        margin-bottom: 30px;
    }
    
    .edit-header h1 {
        font-size: 2rem;
        margin-bottom: 10px;
    }
    
    .edit-form {
        background: white;
        border-radius: 20px;
        padding: 30px;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
    }
    
    .form-group {
        margin-bottom: 25px;
    }
    
    .form-group label {
        display: block;
        font-size: 1.1rem;
        font-weight: 600;
        color: #333;
        margin-bottom: 10px;
    }
    
    .form-group input,
    .form-group textarea,
    .form-group select {
        width: 100%;
        padding: 15px;
        border: 2px solid #e0e0e0;
        border-radius: 12px;
        font-size: 1.1rem;
        transition: border-color 0.3s ease;
    }
    
    .form-group input:focus,
    .form-group textarea:focus,
    .form-group select:focus {
        border-color: #2c3e50;
        outline: none;
    }
    
    .form-group input:disabled,
    .form-group select:disabled {
        background: #f5f5f5;
        color: #999;
        cursor: not-allowed;
    }
    
    .field-locked {
        display: flex;
        align-items: center;
        gap: 8px;
        color: #999;
        font-size: 0.9rem;
        margin-top: 5px;
    }
    
    .field-locked i {
        color: #e74c3c;
    }
    
    .save-btn {
        width: 100%;
        padding: 18px;
        background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%);
        color: white;
        border: none;
        border-radius: 12px;
        font-size: 1.3rem;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s ease;
        margin-top: 20px;
    }
    
    .save-btn:hover {
        transform: translateY(-3px);
        box-shadow: 0 8px 25px rgba(17, 153, 142, 0.4);
    }
    
    .info-box {
        background: #e3f2fd;
        border-radius: 12px;
        padding: 15px 20px;
        margin-bottom: 25px;
        display: flex;
        align-items: flex-start;
        gap: 12px;
    }
    
    .info-box .icon {
        color: #1976d2;
        font-size: 1.3rem;
    }
    
    .info-box p {
        margin: 0;
        color: #333;
        font-size: 1rem;
    }
    
    .back-link {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        color: #666;
        text-decoration: none;
        font-size: 1.1rem;
        margin-top: 30px;
    }
    
    .back-link:hover {
        color: #333;
    }
</style>

<div class="edit-container">
    <div class="edit-header">
        <h1>⚙️ @(isMalayalam ? "പ്രൊഫൈൽ എഡിറ്റ് ചെയ്യുക" : "Edit Profile")</h1>
        <p>@(isMalayalam ? "നിങ്ങളുടെ വിവരങ്ങൾ അപ്ഡേറ്റ് ചെയ്യുക" : "Update your information")</p>
    </div>
    
    <div class="info-box">
        <span class="icon"><i class="fas fa-info-circle"></i></span>
        <p>
            @(isMalayalam 
                ? "ചില ഫീൽഡുകൾ സുരക്ഷാ കാരണങ്ങളാൽ ലോക്ക് ചെയ്തിരിക്കുന്നു. മാറ്റങ്ങൾക്ക് നിങ്ങളുടെ രക്ഷാധികാരിയെ ബന്ധപ്പെടുക."
                : "Some fields are locked for safety reasons. Contact your guardian for changes.")
        </p>
    </div>
    
    <form asp-action="EditProfile" method="post" class="edit-form">
        @Html.AntiForgeryToken()
        
        <!-- Name - Locked -->
        <div class="form-group">
            <label>@(isMalayalam ? "പേര്" : "Full Name")</label>
            <input type="text" value="@Model.FullName" disabled />
            <div class="field-locked">
                <i class="fas fa-lock"></i>
                @(isMalayalam ? "ഈ ഫീൽഡ് മാറ്റാൻ കഴിയില്ല" : "This field cannot be changed")
            </div>
        </div>
        
        <!-- Age - Locked -->
        <div class="form-group">
            <label>@(isMalayalam ? "പ്രായം" : "Age")</label>
            <input type="number" value="@Model.Age" disabled />
            <div class="field-locked">
                <i class="fas fa-lock"></i>
                @(isMalayalam ? "ഈ ഫീൽഡ് മാറ്റാൻ കഴിയില്ല" : "This field cannot be changed")
            </div>
        </div>
        
        <!-- Gender - Locked -->
        <div class="form-group">
            <label>@(isMalayalam ? "ലിംഗഭേദം" : "Gender")</label>
            <input type="text" value="@Model.Gender" disabled />
            <div class="field-locked">
                <i class="fas fa-lock"></i>
                @(isMalayalam ? "ഈ ഫീൽഡ് മാറ്റാൻ കഴിയില്ല" : "This field cannot be changed")
            </div>
        </div>
        
        <!-- Blood Group - Locked -->
        <div class="form-group">
            <label>@(isMalayalam ? "രക്ത ഗ്രൂപ്പ്" : "Blood Group")</label>
            <input type="text" value="@Model.BloodGroup" disabled />
            <div class="field-locked">
                <i class="fas fa-lock"></i>
                @(isMalayalam ? "ഈ ഫീൽഡ് മാറ്റാൻ കഴിയില്ല" : "This field cannot be changed")
            </div>
        </div>
        
        <!-- Emergency Phone - Locked -->
        <div class="form-group">
            <label>@(isMalayalam ? "അടിയന്തര ഫോൺ (രക്ഷാധികാരി)" : "Emergency Phone (Guardian)")</label>
            <input type="text" value="@Model.EmergencyPhone" disabled />
            <div class="field-locked">
                <i class="fas fa-lock"></i>
                @(isMalayalam ? "രക്ഷാധികാരി മാറ്റാൻ കഴിയില്ല" : "Guardian cannot be changed")
            </div>
        </div>
        
        <hr style="margin: 30px 0; border-color: #e0e0e0;" />
        
        <h4 style="color: #11998e; margin-bottom: 20px;">
            <i class="fas fa-edit me-2"></i>
            @(isMalayalam ? "എഡിറ്റ് ചെയ്യാവുന്ന ഫീൽഡുകൾ" : "Editable Fields")
        </h4>
        
        <!-- Medical Conditions - Editable -->
        <div class="form-group">
            <label>@(isMalayalam ? "ആരോഗ്യ പ്രശ്നങ്ങൾ" : "Medical Conditions")</label>
            <textarea asp-for="MedicalConditions" rows="4" placeholder="@(isMalayalam ? "പ്രമേഹം, രക്താതിസമ്മർദ്ദം, ഹൃദ്രോഗം..." : "Diabetes, High BP, Heart conditions...")"></textarea>
        </div>
        
        <!-- Medications - Editable -->
        <div class="form-group">
            <label>@(isMalayalam ? "മരുന്നുകൾ" : "Current Medications")</label>
            <textarea asp-for="Medications" rows="4" placeholder="@(isMalayalam ? "നിലവിൽ കഴിക്കുന്ന മരുന്നുകളുടെ പേരുകൾ..." : "Names of medicines you currently take...")"></textarea>
        </div>
        
        <button type="submit" class="save-btn">
            @(isMalayalam ? "മാറ്റങ്ങൾ സേവ് ചെയ്യുക" : "Save Changes") 💾
        </button>
    </form>
    
    <div class="text-center">
        <a href="@Url.Action("Dashboard", "ElderCare")" class="back-link">
            <i class="fas fa-arrow-left"></i>
            @(isMalayalam ? "ഡാഷ്ബോർഡിലേക്ക് മടങ്ങുക" : "Back to Dashboard")
        </a>
    </div>
</div>
