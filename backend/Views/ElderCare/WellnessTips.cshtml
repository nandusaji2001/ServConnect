@using ServConnect.ViewModels
@{
    ViewData["Title"] = "AI Wellness Recommendations";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var isMalayalam = System.Globalization.CultureInfo.CurrentUICulture.Name.Equals("ml-IN", StringComparison.OrdinalIgnoreCase);
    var healthDetailsFilled = ViewBag.HealthDetailsFilled as bool? ?? false;
    var prediction = ViewBag.Prediction as WellnessPredictionResult;
}

<style>
    .wellness-container {
        width: 100%;
        margin: 0;
        padding: 20px 30px;
    }
    
    .wellness-header {
        text-align: center;
        background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
        color: white;
        padding: 30px;
        border-radius: 20px;
        margin-bottom: 30px;
    }
    
    .wellness-header h1 {
        font-size: 2.2rem;
        margin-bottom: 10px;
    }
    
    .no-data-card {
        background: linear-gradient(135deg, #ffecd2 0%, #fcb69f 100%);
        border-radius: 20px;
        padding: 40px;
        text-align: center;
        margin-bottom: 30px;
    }
    
    .no-data-card .icon {
        font-size: 4rem;
        margin-bottom: 20px;
    }
    
    .no-data-card h2 {
        color: #333;
        margin-bottom: 15px;
    }
    
    .no-data-card p {
        color: #666;
        font-size: 1.1rem;
        margin-bottom: 0;
    }
    
    .section-card {
        background: white;
        border-radius: 20px;
        padding: 25px;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        margin-bottom: 25px;
    }
    
    .section-title {
        display: flex;
        align-items: center;
        gap: 12px;
        font-size: 1.4rem;
        color: #333;
        margin-bottom: 20px;
        padding-bottom: 15px;
        border-bottom: 2px solid #e0e0e0;
    }
    
    .section-title .icon {
        font-size: 1.8rem;
    }
    
    .risk-badge {
        display: inline-block;
        padding: 8px 20px;
        border-radius: 25px;
        font-weight: 600;
        font-size: 1rem;
        text-transform: uppercase;
    }
    
    .risk-low {
        background: #d4edda;
        color: #155724;
    }
    
    .risk-moderate {
        background: #fff3cd;
        color: #856404;
    }
    
    .risk-high {
        background: #f8d7da;
        color: #721c24;
    }
    
    .meal-section {
        margin-bottom: 20px;
    }
    
    .meal-title {
        font-weight: 600;
        color: #4facfe;
        margin-bottom: 10px;
        font-size: 1.1rem;
    }
    
    .meal-items {
        list-style: none;
        padding: 0;
        margin: 0;
    }
    
    .meal-items li {
        padding: 8px 0;
        border-bottom: 1px solid #f0f0f0;
        display: flex;
        align-items: center;
        gap: 10px;
    }
    
    .meal-items li:last-child {
        border-bottom: none;
    }
    
    .meal-items li::before {
        content: "🍽️";
    }
    
    .exercise-card {
        background: #f8f9fa;
        border-radius: 12px;
        padding: 15px;
        margin-bottom: 15px;
    }
    
    .exercise-card h4 {
        color: #333;
        margin-bottom: 8px;
        display: flex;
        align-items: center;
        gap: 8px;
    }
    
    .exercise-meta {
        display: flex;
        gap: 20px;
        margin-bottom: 8px;
        font-size: 0.9rem;
        color: #666;
    }
    
    .exercise-meta span {
        display: flex;
        align-items: center;
        gap: 5px;
    }
    
    .exercise-desc {
        color: #555;
        font-size: 0.95rem;
    }
    
    .tip-list {
        list-style: none;
        padding: 0;
        margin: 0;
    }
    
    .tip-list li {
        padding: 10px 0;
        border-bottom: 1px solid #f0f0f0;
        display: flex;
        align-items: flex-start;
        gap: 10px;
    }
    
    .tip-list li:last-child {
        border-bottom: none;
    }
    
    .tip-list li .icon {
        flex-shrink: 0;
    }
    
    .warning-box {
        background: #fff3cd;
        border-left: 4px solid #ffc107;
        padding: 15px;
        border-radius: 0 10px 10px 0;
        margin-bottom: 15px;
    }
    
    .warning-box p {
        margin: 0;
        color: #856404;
        display: flex;
        align-items: center;
        gap: 10px;
    }
    
    .ai-disclaimer {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        padding: 20px;
        border-radius: 15px;
        text-align: center;
        margin-top: 30px;
    }
    
    .ai-disclaimer .icon {
        font-size: 2rem;
        margin-bottom: 10px;
    }
    
    .ai-disclaimer p {
        margin: 0;
        font-size: 0.95rem;
        opacity: 0.95;
    }
    
    .summary-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
        gap: 15px;
        margin-bottom: 20px;
    }
    
    .summary-item {
        background: #f8f9fa;
        padding: 15px;
        border-radius: 10px;
        text-align: center;
    }
    
    .summary-item .label {
        font-size: 0.85rem;
        color: #666;
        margin-bottom: 5px;
    }
    
    .summary-item .value {
        font-size: 1.3rem;
        font-weight: 600;
        color: #333;
    }
    
    .back-link {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        color: #666;
        text-decoration: none;
        font-size: 1.1rem;
        margin-top: 20px;
    }
    
    .back-link:hover {
        color: #333;
    }
    
    .prediction-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        flex-wrap: wrap;
        gap: 15px;
        margin-bottom: 20px;
    }
</style>

<div class="wellness-container">
    <div class="wellness-header">
        <h1>🧬 @(isMalayalam ? "AI ക്ഷേമ ശുപാർശകൾ" : "AI Wellness Recommendations")</h1>
        <p>@(isMalayalam ? "നിങ്ങളുടെ ആരോഗ്യ വിവരങ്ങൾ അടിസ്ഥാനമാക്കിയുള്ള വ്യക്തിഗത ശുപാർശകൾ" : "Personalized recommendations based on your health data")</p>
    </div>
    
    @if (!healthDetailsFilled)
    {
        <!-- Health details not filled by guardian -->
        <div class="no-data-card">
            <div class="icon">📋</div>
            <h2>@(isMalayalam ? "ആരോഗ്യ വിവരങ്ങൾ ആവശ്യമാണ്" : "Health Details Required")</h2>
            <p>
                @(isMalayalam 
                    ? "AI അടിസ്ഥാനമാക്കിയുള്ള ക്ഷേമ ശുപാർശകൾ ലഭിക്കാൻ നിങ്ങളുടെ രക്ഷാധികാരി ആരോഗ്യ വിവരങ്ങൾ പൂരിപ്പിക്കേണ്ടതുണ്ട്."
                    : "Your guardian needs to fill in your health details to get AI-powered wellness recommendations.")
            </p>
            <p style="margin-top: 15px; font-size: 0.95rem; color: #888;">
                @(isMalayalam 
                    ? "ദയവായി നിങ്ങളുടെ രക്ഷാധികാരിയോട് Guardian Dashboard-ൽ നിന്ന് ആരോഗ്യ വിവരങ്ങൾ പൂരിപ്പിക്കാൻ ആവശ്യപ്പെടുക."
                    : "Please ask your guardian to fill the health details from their Guardian Dashboard.")
            </p>
        </div>
    }
    else if (prediction != null && prediction.Success)
    {
        <!-- Health Summary -->
        <div class="section-card">
            <div class="section-title">
                <span class="icon">📊</span>
                @(isMalayalam ? "നിങ്ങളുടെ ആരോഗ്യ സംഗ്രഹം" : "Your Health Summary")
            </div>
            <div class="summary-grid">
                @if (prediction.Summary != null)
                {
                    <div class="summary-item">
                        <div class="label">@(isMalayalam ? "പ്രായം" : "Age")</div>
                        <div class="value">@(prediction.Summary.Age ?? 0) yrs</div>
                    </div>
                    <div class="summary-item">
                        <div class="label">BMI</div>
                        <div class="value">@(prediction.Summary.BMI?.ToString("F1") ?? "N/A")</div>
                    </div>
                    <div class="summary-item">
                        <div class="label">@(isMalayalam ? "രക്തസമ്മർദ്ദം" : "Blood Pressure")</div>
                        <div class="value">@(prediction.Summary.BloodPressure ?? "N/A")</div>
                    </div>
                    <div class="summary-item">
                        <div class="label">@(isMalayalam ? "കൊളസ്ട്രോൾ" : "Cholesterol")</div>
                        <div class="value">@(prediction.Summary.Cholesterol?.ToString("F0") ?? "N/A")</div>
                    </div>
                }
            </div>
            
            <div class="prediction-header">
                <div>
                    <strong>@(isMalayalam ? "ഹൃദയ അപകട സാധ്യത:" : "Heart Risk Level:")</strong>
                    <span class="risk-badge risk-@prediction.HeartRisk.ToLower()">
                        @prediction.HeartRisk
                    </span>
                </div>
            </div>
        </div>
        
        <!-- Heart Risk & Exercise Recommendations -->
        @if (prediction.HeartDetails != null)
        {
            <div class="section-card">
                <div class="section-title">
                    <span class="icon">❤️</span>
                    @(isMalayalam ? "ഹൃദയാരോഗ്യ ശുപാർശകൾ" : "Heart Health Recommendations")
                </div>
                
                <p style="font-size: 1.1rem; color: #555; margin-bottom: 20px;">
                    @prediction.HeartDetails.RiskDescription
                </p>
                
                @if (prediction.HeartDetails.Warnings != null && prediction.HeartDetails.Warnings.Any())
                {
                    foreach (var warning in prediction.HeartDetails.Warnings)
                    {
                        <div class="warning-box">
                            <p>⚠️ @warning</p>
                        </div>
                    }
                }
                
                <h4 style="margin-top: 25px; margin-bottom: 15px;">🏃 @(isMalayalam ? "ശുപാർശ ചെയ്യുന്ന വ്യായാമങ്ങൾ" : "Recommended Exercises")</h4>
                @foreach (var exercise in prediction.HeartDetails.Exercises)
                {
                    <div class="exercise-card">
                        <h4>🎯 @exercise.Name</h4>
                        <div class="exercise-meta">
                            <span>⏱️ @exercise.Duration</span>
                            <span>📅 @exercise.Frequency</span>
                        </div>
                        <p class="exercise-desc">@exercise.Description</p>
                    </div>
                }
                
                <h4 style="margin-top: 25px; margin-bottom: 15px;">💡 @(isMalayalam ? "ജീവിതശൈലി നുറുങ്ങുകൾ" : "Lifestyle Tips")</h4>
                <ul class="tip-list">
                    @foreach (var tip in prediction.HeartDetails.LifestyleTips)
                    {
                        <li><span class="icon">✅</span> @tip</li>
                    }
                </ul>
            </div>
        }
        
        <!-- Diet Plan -->
        @if (prediction.DetailedDiet != null)
        {
            <div class="section-card">
                <div class="section-title">
                    <span class="icon">🥗</span>
                    @prediction.DetailedDiet.Title
                </div>
                
                <p style="color: #666; margin-bottom: 25px;">
                    @prediction.DetailedDiet.Description
                </p>
                
                <p style="background: #e8f5e9; padding: 12px 15px; border-radius: 10px; margin-bottom: 25px;">
                    <strong>💡 @(isMalayalam ? "AI ശുപാർശ:" : "AI Recommendation:")</strong> @prediction.DietRecommendation
                </p>
                
                <div class="row">
                    <div class="col-md-6">
                        <div class="meal-section">
                            <div class="meal-title">🌅 @(isMalayalam ? "പ്രഭാത ഭക്ഷണം" : "Breakfast")</div>
                            <ul class="meal-items">
                                @foreach (var item in prediction.DetailedDiet.Breakfast.Take(4))
                                {
                                    <li>@item</li>
                                }
                            </ul>
                        </div>
                        
                        <div class="meal-section">
                            <div class="meal-title">☀️ @(isMalayalam ? "ഉച്ച ഭക്ഷണം" : "Lunch")</div>
                            <ul class="meal-items">
                                @foreach (var item in prediction.DetailedDiet.Lunch.Take(4))
                                {
                                    <li>@item</li>
                                }
                            </ul>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="meal-section">
                            <div class="meal-title">🌙 @(isMalayalam ? "അത്താഴം" : "Dinner")</div>
                            <ul class="meal-items">
                                @foreach (var item in prediction.DetailedDiet.Dinner.Take(4))
                                {
                                    <li>@item</li>
                                }
                            </ul>
                        </div>
                        
                        <div class="meal-section">
                            <div class="meal-title">🍎 @(isMalayalam ? "ലഘുഭക്ഷണം" : "Snacks")</div>
                            <ul class="meal-items">
                                @foreach (var item in prediction.DetailedDiet.Snacks.Take(4))
                                {
                                    <li>@item</li>
                                }
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        }
        
        <!-- Dietary Tips from Heart Risk -->
        @if (prediction.HeartDetails != null && prediction.HeartDetails.DietaryTips.Any())
        {
            <div class="section-card">
                <div class="section-title">
                    <span class="icon">🍽️</span>
                    @(isMalayalam ? "ഭക്ഷണ നുറുങ്ങുകൾ" : "Dietary Tips")
                </div>
                <ul class="tip-list">
                    @foreach (var tip in prediction.HeartDetails.DietaryTips)
                    {
                        <li><span class="icon">🥬</span> @tip</li>
                    }
                </ul>
            </div>
        }
        
        <!-- AI Disclaimer -->
        <div class="ai-disclaimer">
            <div class="icon">🤖</div>
            <p>
                <strong>@(isMalayalam ? "AI ജനറേറ്റഡ് ശുപാർശകൾ:" : "AI-Generated Recommendations:")</strong>
                @(isMalayalam 
                    ? "ഈ ശുപാർശകൾ Random Forest Machine Learning മോഡൽ ഉപയോഗിച്ച് നിങ്ങളുടെ ആരോഗ്യ ഡാറ്റ അടിസ്ഥാനമാക്കി സൃഷ്ടിച്ചതാണ്. ഇവ പൊതുവായ മാർഗ്ഗനിർദ്ദേശങ്ങൾ മാത്രമാണ്, പ്രൊഫഷണൽ മെഡിക്കൽ ഉപദേശത്തിന് പകരമല്ല. ഏതെങ്കിലും ഭക്ഷണക്രമമോ വ്യായാമ പരിപാടിയോ ആരംഭിക്കുന്നതിന് മുമ്പ് ദയവായി നിങ്ങളുടെ ഡോക്ടറുമായി ബന്ധപ്പെടുക."
                    : "These recommendations are generated using a Random Forest Machine Learning model based on your health data. They are general guidelines only and not a substitute for professional medical advice. Please consult your doctor before starting any diet or exercise program.")
            </p>
        </div>
    }
    else if (prediction != null && !prediction.Success)
    {
        <div class="section-card" style="text-align: center; padding: 40px;">
            <div style="font-size: 3rem; margin-bottom: 15px;">⚠️</div>
            <h3>@(isMalayalam ? "ശുപാർശകൾ ലഭ്യമല്ല" : "Recommendations Unavailable")</h3>
            <p style="color: #666;">
                @(isMalayalam 
                    ? "AI സേവനം താൽക്കാലികമായി ലഭ്യമല്ല. ദയവായി പിന്നീട് വീണ്ടും ശ്രമിക്കുക."
                    : "The AI service is temporarily unavailable. Please try again later.")
            </p>
            @if (!string.IsNullOrEmpty(prediction.Error))
            {
                <p style="color: #999; font-size: 0.9rem;">Error: @prediction.Error</p>
            }
        </div>
    }
    
    <div class="text-center">
        <a href="@Url.Action("Dashboard", "ElderCare")" class="back-link">
            <i class="fas fa-arrow-left"></i>
            @(isMalayalam ? "ഡാഷ്ബോർഡിലേക്ക് മടങ്ങുക" : "Back to Dashboard")
        </a>
    </div>
</div>
